# Pattern D — source-only: sleep uses nan + node-gyp, so it always compiles locally (no prebuilt artifacts).
# strategy is always SOURCE_BUILD (environment-independent); risk depends on whether the local toolchain is complete →
# LOW / MEDIUM with a compiler, HIGH without one, so the expectation does not pin risk.
# Lockfile generated by `npm install --package-lock-only sleep@6.3.0`.
pattern: SourceOnly
strategy: SOURCE_BUILD
native: true
network_calls: 0
subject: sleep
