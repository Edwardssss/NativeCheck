# Pattern D — source-only: cpu-features uses nan + node-gyp (an optional accelerator for ssh2); it always compiles.
# strategy is always SOURCE_BUILD (environment-independent); risk depends on the toolchain → not pinned.
# Lockfile generated by `npm install --package-lock-only cpu-features@0.0.10`.
pattern: SourceOnly
strategy: SOURCE_BUILD
native: true
network_calls: 0
subject: cpu-features
