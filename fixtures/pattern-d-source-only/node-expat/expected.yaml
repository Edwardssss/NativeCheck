# Pattern D — source-only: node-expat uses nan + bindings + node-gyp, so it always compiles locally.
# strategy is always SOURCE_BUILD (environment-independent); risk depends on the toolchain → not pinned.
# Lockfile generated by `npm install --package-lock-only node-expat@2.4.1`.
pattern: SourceOnly
strategy: SOURCE_BUILD
native: true
network_calls: 0
subject: node-expat
