# 模式 D —— source-only：node-expat 用 nan + bindings + node-gyp，必然本地源码编译。
# strategy 恒为 SOURCE_BUILD（环境无关）。risk 依赖工具链是否齐全 → 不 pin（环境相关）。
# 真实 lockfile 由 `npm install --package-lock-only node-expat@2.4.1` 生成。
pattern: SourceOnly
strategy: SOURCE_BUILD
native: true
network_calls: 0
subject: node-expat
