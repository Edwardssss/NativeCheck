# Pattern D — source-only: sse4_crc32 builds with bindings + node-addon-api + node-gyp rebuild and
# has no node-gyp-build / prebuild-install, so it always compiles (N-API, so modern Node is fine).
# strategy is always SOURCE_BUILD (environment-independent); risk depends on the toolchain → not pinned.
pattern: SourceOnly
strategy: SOURCE_BUILD
native: true
network_calls: 0
subject: sse4_crc32
