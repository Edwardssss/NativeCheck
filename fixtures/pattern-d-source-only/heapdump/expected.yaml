# Pattern D — source-only: heapdump@0.3.15 uses nan, so it compiles (a V8 heap snapshot addon).
# pattern is always SourceOnly and strategy always SOURCE_BUILD (both environment-independent);
pattern: SourceOnly
strategy: SOURCE_BUILD
native: true
network_calls: 0
subject: heapdump
