# Pattern D — source-only: murmurhash3 is a plain node-gyp rebuild (no node-gyp-build / prebuild-install).
# strategy is always SOURCE_BUILD; risk depends on the toolchain, so it is never pinned.
pattern: SourceOnly
strategy: SOURCE_BUILD
native: true
network_calls: 0
subject: murmurhash3
