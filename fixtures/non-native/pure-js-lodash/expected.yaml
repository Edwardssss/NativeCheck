# Non-native control: pure-JS lodash must produce no native candidates (nativeCandidates=0).
# Lockfile generated by `npm install --package-lock-only lodash@4.17.21`.
native: false
native_count: 0 # asserts no native candidates (as opposed to candidates that are not native)
network_calls: 0
