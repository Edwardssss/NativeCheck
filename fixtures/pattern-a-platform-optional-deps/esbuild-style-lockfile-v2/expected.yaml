# Pattern A — esbuild style, **lockfileVersion 2** variant (v2 is a superset of v3).
# Identical semantics and verdict to esbuild-style; only the lockfile format differs, proving
# that arborist's loadVirtual reads the whole platform cluster under v2 and classifies it
# correctly. Lockfile generated by `npm install --package-lock-only --lockfile-version=2 esbuild@0.25.0`.
pattern: PlatformOptionalDeps
strategy: PREBUILT
risk: LOW
native: true
network_calls: 0
subject: esbuild
