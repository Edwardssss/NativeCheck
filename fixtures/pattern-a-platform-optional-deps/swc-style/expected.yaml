# Pattern A — @swc/core style (platform sub-packages such as @swc/linux-x64-gnu plus a postinstall script).
# Lockfile generated by `npm install --package-lock-only @swc/core@1.10.0`.
pattern: PlatformOptionalDeps
strategy: PREBUILT
risk: LOW
native: true
network_calls: 0
subject: '@swc/core'
