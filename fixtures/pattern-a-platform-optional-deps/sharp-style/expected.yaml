# 模式 A —— sharp 风格（无 install 脚本，平台子包 @img/sharp-* 驱动），防漏报。
# 真实 lockfile 由 `npm install --package-lock-only sharp@0.34.0` 生成。
pattern: PlatformOptionalDeps
strategy: PREBUILT
risk: LOW
native: true # 关键：纯 JS 检测器会漏报 sharp（无 install 脚本）
network_calls: 0 # 关键：断言零网络，防止默认路径退化成联网
subject: sharp
