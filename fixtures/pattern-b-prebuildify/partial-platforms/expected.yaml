# Pattern B — prebuildify partial-platforms: microtime also uses node-gyp-build (B).
# Also UNVERIFIED under fast mode (--deep is what tells you about this platform's artifact).
# Lockfile generated by `npm install --package-lock-only microtime@3.1.1`.
pattern: Prebuildify
strategy: PREBUILT
risk: UNVERIFIED
native: true
network_calls: 0
subject: microtime
