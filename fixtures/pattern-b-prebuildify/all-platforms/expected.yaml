# Pattern B — prebuildify all-platforms: prebuilt artifacts inside the package's own tarball under prebuilds/.
# better-sqlite3@13 uses the node-gyp-build picker (B, not a compile). Under fast mode the
# prebuilds/ manifest requires reading the tarball (--deep streaming forensics) → honestly
# UNVERIFIED, never guessed as HIGH. Lockfile generated by `npm install --package-lock-only better-sqlite3@13.0.3`.
pattern: Prebuildify
strategy: PREBUILT
risk: UNVERIFIED # no forensics under fast mode → UNVERIFIED (--deep turns a hit into LOW)
native: true
network_calls: 0
subject: better-sqlite3
