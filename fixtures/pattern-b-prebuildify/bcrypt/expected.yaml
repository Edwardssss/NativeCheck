# 模式 B —— prebuildify：bcrypt 依赖 node-addon-api + node-gyp-build（B）。
# node-gyp-build 是挑选器（非 node-gyp 编译器），产物打进自身 tarball prebuilds/。
# fast 下是否含当前平台产物未验证 → UNVERIFIED（--deep 才知道）。
# 真实 lockfile 由 `npm install --package-lock-only bcrypt@6.0.0` 生成。
pattern: Prebuildify
strategy: PREBUILT
risk: UNVERIFIED
native: true
network_calls: 0
subject: bcrypt
