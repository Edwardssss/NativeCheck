# Pattern B — prebuildify: bcrypt depends on node-addon-api + node-gyp-build (B).
# node-gyp-build is a picker, not the node-gyp compiler, and the artifacts ship inside the package's own tarball.
# Under fast mode whether an artifact for this platform exists is unknown → UNVERIFIED (--deep decides).
# Lockfile generated by `npm install --package-lock-only bcrypt@6.0.0`.
pattern: Prebuildify
strategy: PREBUILT
risk: UNVERIFIED
native: true
network_calls: 0
subject: bcrypt
