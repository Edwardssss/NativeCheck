# bun.lockb is a supported format now (see the testdata/bun-lockb integration test).
# This fixture is a **malformed bun.lockb** (random bytes, invalid header) → parseBunLockfile
# throws → ingest returns unsupported (fail closed); the assertion checks the reason mentions bun.
unsupported: true
detected: bun.lockb
reason_contains: bun
network_calls: 0
