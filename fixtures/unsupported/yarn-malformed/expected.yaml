# yarn.lock is a supported format now (see the testdata/yarn-v1 and yarn-berry integration tests).
# This fixture is a **malformed yarn.lock** (no valid top-level entry) → parseYarnLockfile returns
# empty → ingest returns unsupported (fail closed); the assertion checks the reason mentions yarn.
unsupported: true
detected: yarn.lock
reason_contains: yarn
network_calls: 0
