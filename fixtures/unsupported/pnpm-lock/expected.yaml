# pnpm-lock.yaml is a supported format now (see the testdata/pnpm integration test).
# This fixture is an incomplete pnpm lockfile (packages without snapshots) → parsePnpmLockfile
# returns empty → ingest returns unsupported (fail closed); the assertion checks the reason
unsupported: true
detected: pnpm-lock.yaml
reason_contains: pnpm
network_calls: 0
