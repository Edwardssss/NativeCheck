# Unsupported format: lockfile v1 → exit explicitly rather than guess (fail closed).
# Asserts scan returns report.unsupported and that the reason mentions v1.
unsupported: true
detected: package-lock.json v1
reason_contains: v1
network_calls: 0
