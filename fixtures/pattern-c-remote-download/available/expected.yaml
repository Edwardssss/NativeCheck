# 模式 C —— remote-download available：better-sqlite3@11 用 prebuild-install（C）。
# fast 下远端产物未验证 → UNVERIFIED（--deep 发 HEAD，200→LOW）。
# 注意本样本含 2 个 finding：better-sqlite3(native,C) + prebuild-install(工具，NotNative)；
# 用 subject 只断言 native 入口 better-sqlite3。
# 真实 lockfile 由 `npm install --package-lock-only better-sqlite3@11.10.0` 生成。
pattern: RemoteDownload
strategy: PREBUILT
risk: UNVERIFIED
native: true
network_calls: 0
subject: better-sqlite3
