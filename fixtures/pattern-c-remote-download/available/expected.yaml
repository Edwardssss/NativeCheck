# Pattern C — remote download: better-sqlite3@11 uses prebuild-install (C).
# Under fast mode the remote artifact is unverified → UNVERIFIED (--deep sends the HEAD: 200 → LOW).
# Note the two findings: better-sqlite3 (native, C) plus prebuild-install (a tool, NotNative);
# subject pins the assertion to the native entry point, better-sqlite3.
# Lockfile generated by `npm install --package-lock-only better-sqlite3@11.10.0`.
pattern: RemoteDownload
strategy: PREBUILT
risk: UNVERIFIED
native: true
network_calls: 0
subject: better-sqlite3
