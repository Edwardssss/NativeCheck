# Pattern C — remote download: keytar depends on node-addon-api + prebuild-install (C).
# Same mechanism as canvas: prebuild-install fetches the prebuilt binary from a remote host.
# Under fast mode the remote artifact is unverified → UNVERIFIED (--deep sends the HEAD: a hit → LOW).
# Note the two findings: keytar (native, C) plus prebuild-install (a tool, NotNative);
# subject pins the assertion to the native entry point, keytar.
# Lockfile generated by `npm install --package-lock-only keytar@7.9.0`.
pattern: RemoteDownload
strategy: PREBUILT
risk: UNVERIFIED
native: true
network_calls: 0
subject: keytar
